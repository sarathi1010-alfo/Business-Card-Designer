# Google Search Console (GSC) Technical Fix Plan

## Crawled - currently not indexed
The following legacy pages are crawled but not indexed:
- `/templates/legacy-test-1`
- `/templates/legacy-test-2`
- `/templates/legacy-test-3`

**Action Plan:**
1. Internal Links: We will ensure these templates are linked from our new, high-authority Tier 1 blog articles (e.g. "How to Create a Digital Business Card That Converts").
2. Content Decay Refresh: We will update content in related hub pages pointing to these templates.
3. Re-crawling: Submit these URLs directly in the GSC URL Inspection tool to trigger a re-crawl.
4. Robots.txt: Verify these are not blocked (verified - they are not).

Once re-crawled with the new inbound links from high authority pages, their perceived value will rise, encouraging Google to index them.
